/**
 * DeepJelly Global Constants
 *
 * Meta-Name: Visual & Animation Configuration Constants
 * Meta-Description: Defines core visual specifications and animation parameters for the DeepJelly character display system.
 */

/**
 * Character display dimensions (in pixels)
 */
export const CHARACTER_DEFAULT_SIZE = 500;
export const CHARACTER_MIN_SIZE = 250; // 50% of default
export const CHARACTER_MAX_SIZE = 1000; // 200% of default

/**
 * Animation frame rate (frames per second)
 */
export const ANIMATION_FRAME_RATE = 24;

/**
 * Character display scale (zoom level)
 */
export const CHARACTER_DEFAULT_SCALE = 1.0;
export const CHARACTER_MIN_SCALE = 0.5;
export const CHARACTER_MAX_SCALE = 2.0;
